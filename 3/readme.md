### Lama bytecode idioms analyser
Make sure `lamac` is installed on your system.

#### Initialize the git submodule
```bash
git submodule update --init --recursive
```

#### Build
```bash
make lamaa
```